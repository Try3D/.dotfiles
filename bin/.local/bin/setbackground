#!/usr/bin/sh

cd ~/.local/share/backgrounds/
currentbg=`awk '{print $4}' ~/.fehbg | tr -d "'"`

if [ "$1" = "-r" -o "$1" = "--random" ]; then
    SELECTED=`shuf -e * | shuf -n 1`
else
    SELECTED=`shuf -e * | fzf`
fi

feh --bg-fill $SELECTED

notify-send "Changed background"
