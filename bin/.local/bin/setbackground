#!/usr/bin/sh

cd ~/.local/share/backgrounds/

if [ "$1" = "-r" -o "$1" = "--random" ]; then
    SELECTED=`shuf -e * | shuf -n 1`
else
    SELECTED=`shuf -e * | rofi -dmenu`
fi

 --bg-fill $SELECTED

killall swaybg

swaybg -i $SELECTED -m fill
